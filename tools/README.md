# Tool binaries

Place external Java tool jars here.

Expected default:

```text
tools/evosuite-1.2.0.jar
tools/apache-maven-3.9.16/
```

`evosuite-1.2.0.jar` is the EvoSuite release jar used by the runner.
`apache-maven-3.9.16/` is the local Maven distribution used by the scripts.
